# AWS Amplify Environment Variables
# Add these in your Amplify Console > App Settings > Environment Variables

# For AWS Amplify deployment (primary)
REACT_APP_API_URL=https://your-api-gateway-id.execute-api.region.amazonaws.com/prod
REACT_APP_DEPLOYMENT_STRATEGY=amplify

# Database credentials (if using RDS)
DB_USER=your-db-user
DB_HOST=your-rds-endpoint
DB_DATABASE=your-database-name
DB_PASSWORD=your-password
DB_PORT=5432
