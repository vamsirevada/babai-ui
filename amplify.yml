version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm install
    start:
      commands:
        - npm start --workspace=backend
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build --workspace=frontend
  artifacts:
    baseDirectory: frontend/dist
    files:
      - '**/*'
