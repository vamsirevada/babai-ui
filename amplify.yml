version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - npm cache clean --force
        - rm -f package-lock.json
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: frontend/dist
    files:
      - '**/*'
