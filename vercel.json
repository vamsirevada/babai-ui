{
  "buildCommand": "rm -rf node_modules package-lock.json frontend/node_modules frontend/package-lock.json && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/review-order/:id",
      "destination": "/api/review-order?id=:id"
    },
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ]
}
