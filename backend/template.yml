# This AWS SAM template has been generated from your function's configuration. If
# your function has one or more triggers, note that the AWS resources associated
# with these triggers aren't fully specified in this template and include
# placeholder values. Open this template in AWS Infrastructure Composer or your
# favorite IDE and modify it to specify a serverless application with other AWS
# resources.
AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: An AWS Serverless Application Model template describing your function.
Resources:
  babaiapi:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Description: ''
      MemorySize: 128
      Timeout: 3
      Handler: index.handler
      Runtime: nodejs20.x
      Architectures:
        - x86_64
      EphemeralStorage:
        Size: 512
      Environment:
        Variables:
          DB_DATABASE: test
          DB_HOST: db.bab-ai.com
          DB_PASSWORD: ecq5UmGxgjqDFYZPDZFW
          DB_PORT: '5432'
          DB_USER: babai_admin
      EventInvokeConfig:
        MaximumEventAgeInSeconds: 21600
        MaximumRetryAttempts: 2
      FunctionUrlConfig:
        AuthType: NONE
        InvokeMode: BUFFERED
        Cors:
          AllowCredentials: false
          AllowOrigins:
            - 'https://www.bab-ai.com'
            - 'https://bab-ai.com'
            - 'https://main.d3u6tp5amhwi3s.amplifyapp.com'
            - 'http://localhost:8080'
          AllowHeaders:
            - 'Content-Type'
            - 'Authorization'
            - 'X-Requested-With'
            - 'Accept'
          AllowMethods:
            - 'GET'
            - 'POST'
            - 'PUT'
            - 'DELETE'
            - 'OPTIONS'
          MaxAge: 300
      PackageType: Zip
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - logs:CreateLogGroup
              Resource: arn:aws:logs:us-east-1:668227157847:*
            - Effect: Allow
              Action:
                - logs:CreateLogStream
                - logs:PutLogEvents
              Resource:
                - >-
                  arn:aws:logs:us-east-1:668227157847:log-group:/aws/lambda/babai-api:*
            - Effect: Allow
              Action:
                - ec2:CreateNetworkInterface
                - ec2:DeleteNetworkInterface
                - ec2:DescribeNetworkInterfaces
              Resource: '*'
      RecursiveLoop: Terminate
      SnapStart:
        ApplyOn: None
      VpcConfig:
        SecurityGroupIds:
          - sg-0fd3535c214245b0e
        SubnetIds:
          - subnet-02daf2f5527864b9e
          - subnet-0b2cc768e357a0623
          - subnet-05393d232ab6ca0ee
        Ipv6AllowedForDualStack: false
      RuntimeManagementConfig:
        UpdateRuntimeOn: Auto
