# BAB AI Backend Environment Configuration
# Copy this to .env and configure all database settings

NODE_ENV=development
PORT=4000

# Database Configuration - REQUIRED
# You must configure these settings for the backend to work

# Local PostgreSQL example:
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=babai_dev
DB_USER=postgres
DB_PASSWORD=your_password

# Cloud database example (uncomment and update):
# DB_HOST=your-cloud-db-host.amazonaws.com
# DB_PORT=5432
# DB_DATABASE=babai_production
# DB_USER=babai_admin
# DB_PASSWORD=your_secure_password

# AWS Configuration (for production deployment)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
