# Backend Environment Variables (.env)
# Copy this to packages/backend/.env

# Server Configuration
NODE_ENV=development
PORT=8080

# Database Configuration (Individual Parameters)
DB_USER=your-db-username
DB_HOST=your-db-host
DB_DATABASE=your-db-name
DB_PASSWORD=your-db-password
DB_PORT=5432

# Alternative: Connection String Format
# DATABASE_URL=postgresql://username:password@host:port/database

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# WhatsApp API Configuration
WHATSAPP_API_TOKEN=your-whatsapp-api-token
WHATSAPP_PHONE_NUMBER_ID=your-phone-number-id

# AWS Configuration (if using AWS services)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key

# Production values (for deployment):
# NODE_ENV=production
# PORT=8080
# DATABASE_URL=postgresql://admin:password@your-rds-endpoint:5432/babai
# CORS_ORIGIN=https://your-frontend.amplifyapp.com
# JWT_SECRET=your-production-jwt-secret
